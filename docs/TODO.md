# DO MVP TODO

**技术框架(已定,2026-09-13)**:TypeScript + React 19 + Vite + Motion + Phosphor;纯前端无后端;数据存 IndexedDB(idb-keyval,图片存原始 Blob),设置与 AI 配置存 localStorage;AI 走 lib/ai adapter → OpenAI 兼容接口直连(key 存本机);部署 GitHub Pages + Actions。

每个窗口开工时发这句 + 对应任务编号即可:
**「先读 AGENTS.md、docs/CONTEXT.md、docs/design/DESIGN.md,然后完成 docs/TODO.md 的任务 #N,完成后提交并推送」**

## 第一步(串行,一个窗口做完,期间不要开其他窗口)

- [x] **#1 工程重构** — 把 77 行单文件 app.jsx 拆成 src/ 模块结构并以 TypeScript 实现(pages / components / store / lib,数据契约类型定义在 store/types.ts),功能与视觉保持完全不变。
- [x] **#2 数据层** — 按文末数据契约实现全局 store;DO/记录/图片存 IndexedDB(idb-keyval,图片存原始 Blob),设置与 AI 配置存 localStorage;把现有硬编码数据换成真实数据流,刷新不丢。

## 第二步(可并行,一行一个窗口)

- [x] **#3 记录创建** — 新增全局记录入口和创建页(文字+图片),「保存」为私人记录,可选关联某个 DO,关联后该 DO 自动变已记录。
- [x] **#4 帮我整理**(前置 #3)— 记录保留原话,用户点「帮我整理」才生成整理版(先接 mock,契约见 docs/DO-PROMPT.md),整理版直接进入可编辑状态。
- [x] **#5 首页 DO 管理** — 主 DO 推荐 1 条 + 备选 1 条 + 收起区(候选上限 3);行动页三选(马上做 / 等等 / 不想做了,2026-09-13 由「现在开始/先放着+意向三选」收敛而来,意向由动作推导);待定超 24h 自动收进历史清单。
- [x] **#7 回忆页真实化** — 删除硬编码记录,按时间倒序展示真实记录(图文两种形态),补空状态。
- [x] **#8 真实 AI** — 把关键词模拟换成真实 AI:OpenAI 兼容接口直连,baseURL / model / key 在设置页可配(key 存 localStorage),mock 保留为无 key 时的兜底;行为契约见 docs/DO-PROMPT.md,支持开放式输入、理解纠正、建议修正。
- [x] **#9 Bug 修复** — 修 DESIGN.md §7 四项:日期硬编码、薄雾/夜色背景失效、回忆页「生成分享内容」按钮直接移除(分享后置)、首页背景与回忆图复用。
- [x] **#10 部署** — vite build + 托管(推荐 GitHub Pages)+ push 自动部署,手机浏览器可访问。
- [x] **#12 数据备份** — 设置页加「导出 JSON / 导入 JSON」,把本机全部 DO 与记录打包备份与恢复(无云同步阶段的唯一保险);#2 启动时申请 navigator.storage.persist()。

## 收尾(全部完成后)

- [x] **#11 真机验收** — iOS Safari + 安卓 Chrome 走通完整闭环:输入 → 行动 → 记录 → 保存 → 回看。(2026-09-13 用户确认通过,一期 MVP 交付)

## 后置(本轮 MVP 不做)

- **分享卡片**:从记录生成单张图文卡片、可编辑、可保存为图片(原 #6,2026-09-13 决定后置)。→ 已立项为二期 #14。

## 二期(2026-09-13 立项,范围:用户五点决策)

> 二期 intent:补完「记录值得被留下」的后半程——记录可撰写、可修改,并能以高质量卡片被表达;AI 对话补回被砍掉的自然修正回路,仍守住「一步原则」。多窗口认领规则同一期。

- [x] **#13 记录撰写**(窗口 A)— 记录页草稿暂存与恢复(IndexedDB,写一半切走不丢);回忆页点记录进入二次编辑/补充(改文字、换图、改关联,关联变更正确联动 DO 状态);保存后再整理(编辑态可再点「帮我整理」);选图即压缩(长边 1600px)与图片加载失败占位;顺手修 DESIGN §7.5 记录页浅色对比度。(2026-09-13 完成)
- [x] **#14 记录卡片导出**(前置 #13,窗口 A 续)— 回忆页每条记录可「导出卡片」:单张图文卡片 v1(封面=首图方图裁切,正文=整理版||原话,日期,关联 DO 念头,浅色固定),先做 1 套模板;预览 + 保存为 PNG(canvas 手绘,html-to-image 在嵌入式 webview 挂起弃用);文案不做二次 AI 润色,直接用 refined/text 保留原语气;无图记录走纯文排版。(2026-09-13 完成)
- [x] **#15 AI 对话规划 DO**(窗口 B,与 #13/#14 无文件交集)— 输入页升级对话式:AI 只在「不同解释会明显改变行动」时问一个简短问题(给建议选项,也可自由回复),最多 2 轮,然后产出单一最小行动进入行动页三选;DO 的 thought 仍存用户原话;lib/ai 增多轮 messages 接口 + mock 对话兜底;DO-PROMPT.md 增补对话契约;严禁生成完整计划。(2026-09-13 完成)

## 三期(2026-09-14 立项,范围:图片体验)

> 三期 intent:把「记录值得被留下」落到看得清——卡片封面不再裁掉画面,多图记录能像小红书一样全屏翻看。多窗口认领规则同一期。

- [x] **#16 卡片封面原比例**(窗口 A)— #14 卡片封面由 1:1 居中裁切改为原图比例绘制(宽度固定 296,高度随比例伸缩,卡片总高动态,暂不设上限);无图记录不受影响。(2026-09-14 完成)
- [x] **#17 回忆页全屏看图**(窗口 A,与 #16 同 change)— 分区响应:点图片区进全屏查看器,点文字区仍进编辑态;查看器黑底、scroll-snap 左右滑动切换、页码 n/N、单图也进、内留编辑入口、加载失败占位复用;不做排序/封面选择/缩放(留待后续)。(2026-09-14 完成;真机滑动手感待走查)
- [x] **#18 列表只展示首图**(窗口 A)— 多图记录在回忆列表只渲染首图缩略 + 右下角标显示总张数,点封面进查看器横滑全部;由 #17 上线后用户反馈收敛;时间行与导出按钮重叠已修。(2026-09-14 完成)

## 四期(2026-09-16 立项,范围:手感与导航)

> 四期 intent:让已经跑通的闭环「好摸」——两页之间能顺手滑动切换,每一次点按与出现都有回应,底部导航不再把屏幕切成两块。多窗口认领规则同一期。

- [x] **#23 横滑切换 + 交互动效 + Tab 栏一体**(窗口 C)— ① 首页改为今天/回忆横向分页器:原生 scroll-snap 左右滑动切换,与 Tab 栏双向同步,两页各自保留纵向滚动位置,未激活页 inert;② 补齐全站交互动效并把参数收敛进 `src/lib/motion.ts`(进入上浮/错峰/按下高亮/呼吸等待/平滑滚到底),见 DESIGN §5;③ 底部 Tab 栏 76→60px、去掉分隔线与独立底色,与页面同层。(2026-09-16 完成;真机滑动手感待走查)
  - 流程说明(2026-09-16 补):原写「未走 SDLC change 直接执行」的豁免——**该豁免按 #20 自己的判据无效**(防复发去向指向尚未落地的 #21/#22)。已补建 change `.sdlc/changes/interaction-motion-h5/` 的六件套(as-built 补录),**#23–#32 同属该 change**;两轴 code review 结论见其 test.md,工具与流程缺陷(含两处工具 bug)见其 maintain.md。另:SDLC 的运行时门禁已于同日补上(`CODEBUDDY.md` + `.codebuddy/rules/sdlc-gate/RULE.mdc`),不再需要靠豁免。
- [x] **#24 交互动效重做:原点 / 方向 / 物理**(窗口 C,与 #23 同 change)— 用户校正「问题不是幅度,是交互动画本身」。① **容器变换**:`+` → 记录页、DO 按钮 → 输入页、回忆条目 → 编辑页,新屏幕从被点控件的形状铺满整屏,返回时按相反曲线收回;② **方向修正**:删掉 Tab 页整块竖向落位,大标题改为按页面位置固定的横向回弹(今天从左、回忆从右),切页动效一律横向;③ **首页常驻**在 push 屏下方(`.push-layer`),容器变换外面露出真实上一屏而不是空白底;④ `goHome` 不再强制切回今天。原则已写进 DESIGN §5.0。(2026-09-16 完成;真机走查待补)
- [x] **#25 每个交互按它做的事设计动效**(窗口 C,与 #23/#24 同 change)— 用户校正「有些交互动画应该是不一样的,就像点击 user 那里的动画也应该是不一样的」。① **外观面板从头像展开**(去掉与头像毫无空间关系的底部上滑),关闭收回头像;② **「最近的 DO」行 → 行动页**补上容器变换(此前漏了,是唯一还会「自己滑进来」的入口);③ **全屏查看器从缩略图扩开**、关闭收回;④ 新增 `navDirection`,**返回输入页时从左侧进入**(此前前进后退都从右侧进,方向是错的)。§5.3 整理成「交互 → 它做的事 → 动效设计 → 原点」对照表。(2026-09-16 完成;真机走查待补)
- [x] **#26 跟手 + 背景修正 + 面板改形**(窗口 C,与 #23–#25 同 change)— 用户三条校正:① **删掉「大标题横向回弹」**——它是「页面停稳后再补一段动画」,与手指脱钩,被读成「页面重新刷新」,判定为设计错误并当天删除;切页位移完全交回原生横滑,tab 落点改为**越过中点立刻翻**让指示点跟手(程序化滚动加 600ms 守卫 + 按下即作废)。② **修 push 屏背景**:`.phone-app.has-background .page` 的毛玻璃透出的是常驻首页的内容(一团高斯模糊鬼影),磨砂只留给 `.main-page`,push 屏改不透明。③ **外观面板改形**:上一轮只换入场动画、形态仍是底部抽屉,判定「换动画救不了错的形态」;改成**挂在头像下方的右上浮层**(`transform-origin: 100% 0%` 对齐头像右下角)。(2026-09-16 完成;真机走查待补)

- [x] **#27 大标题「惯性甩出」取代位移式回弹**(窗口 C,与 #23–#26 同 change)— 用户自己定位根因:「你把字体拉到最左或者最右边回弹回来,但实际不应该是原位弹出去弹回来吗?」——上一版把偏移写进了**静止态**。重做:新增 `lib/use-swipe-lag.ts`,**静止态恒为原位**,偏移量跟随滚动速度(慢拖几乎不甩、快甩才明显)、欠阻尼弹簧归零时过冲一次即「弹回来」、滚动静默 90ms 自动收回(按住不动也在原位)。DESIGN §5.0 补「区分静止态与运动瞬态」。(2026-09-16 完成;真机走查待补)

- [x] **#28 外观升级为全屏页 + macOS 式窗口展开**(窗口 C,与 #23–#27 同 change)— 用户提议「user 这个要不直接展开全屏?展开的动画试试 macOS 那种软件窗口展开和收缩」。① 外观由「右上角浮层」升级为**一整屏 push 屏**:`appearanceOpen` 布尔量收敛成 `Screen = 'appearance'`,进 `.push-layer`,首页自动 `inert`;② 新增 `lib/use-window-deploy.ts`:macOS 式窗口展开,`transform-origin: 0 0` 下用 `translate + scaleX/Y` 把左上角送到控件位置、尺寸缩到控件大小,内容跟着一起放大;点「完成」收缩回头像再切页;③ `screen-origin.ts` 从「四条内距」重构为「矩形 + 框尺寸」,裁切铺满与窗口展开共用同一份测量。(2026-09-16 完成;真机走查待补)

- [x] **#29 外观页切回裁切铺满 + 背景退后(高斯模糊)**(窗口 C,与 #23–#28 同 change)— 用户两条反馈:① **「这个 user 的展开不如 + 号这种展开」**——外观页从 macOS 式窗口缩放切回**裁切铺满**(`use-window-deploy` 保留为未启用备选,附原理说明),判定一整屏下「内容不变形」比「窗口放大」观感好;② **「展开收缩动画背景好像没有高斯模糊的过渡」**——补上「背景退后」:`.main-page.receded` 模糊 12px + 放大 1.04(放大是为了把全幅 blur 的四边虚边推出框外),展开 .42s 与铺开同步、收回 .34s 与收回同步(`:has(.bloom-out)` 让背景直接感知前景在退,不把动画阶段同步进 store)、另有 .24s 兜底覆盖保存后直接切回首页这类无收回动画的路径。(2026-09-16 完成;真机走查待补)

- [x] **#30 圆形控件圆形展开 + 外观页导航对齐**(窗口 C,与 #23–#29 同 change)— 用户三条反馈:① **「user 为什么只有完成按钮?」**——外观页导航改为**左侧返回箭头 + 右侧留空**,与其它 push 屏一致(设置即时生效,「完成」与「返回」是同一个动作,不留两个按钮);② **「+ 号是圆形的,那么展开和回收应该也是圆形的吧」**——确立**形状守恒**:`captureExpandOrigin` 增加 `shape` 声明,圆形控件(`+` 号、头像)起点终点都用 `circle()`(22px → 到最远角),全程是一个圆长大。原先 `inset(... round 22px)` → `inset(0 round 0)` 两端之间圆角被拉平,**途中退化成圆角方块**;③ 删除弃用的 `lib/use-window-deploy.ts`。(2026-09-16 完成;真机走查待补)

- [x] **#31 外观页改草稿模型 + 取消/确认**(窗口 C,与 #23–#30 同 change)— 用户「应该给一个取消/确认,避免改了就改不回去了」。① `theme/background/ai` 三项改为本地草稿,面板内所有改动不写 store;「确认」写回再离开,「取消」直接离开(**真正的空操作**,不写回也不反向 dispatch,不存在"撤销失败");离开只有这两个出口,不会悄悄丢改动;② 导航栏改为「取消 / 外观 / 确认」;③ 数据分组(导入导出)留在草稿之外并在页面内注明,避免"取消能撤销导入"的误解。(2026-09-16 完成;真机走查待补)

## 五期(2026-09-17 立项,范围:UI V2 换代)

> 五期 intent:用户拿到新设计稿(`UI/2026-09-17-19-51-33/DO-UI` 六屏),要求「按这一套重新复刻出来,这个版本就是 V2 版」。
> 视觉与信息架构整体换代,产品语义不动。全部任务同属 change `.sdlc/changes/ui-v2-redesign/`。

- [x] **#33 V2 设计系统 + 出发页** — 色板、超窄体大字与 kicker 排版、胶囊形状系统落到 `styles.css`;新增冷启动出发页,`settings.onboarded` 控制只出现一次。(2026-09-17 完成)
- [x] **#34 今天页 V2** — 墨黑念头卡(输入 + 示例 chip + 蜜桃胶囊)、正在进行卡(封面 + 尝试次数 + 最长时长)、最近的痕迹两行。(2026-09-17 完成)
- [x] **#35 第一步页 V2** — 黑卡超窄体大字(时长 + MIN/HR)+ 三档时长胶囊 + 「换一个建议」;三选保留三档,第三档降为弱文字按钮。(2026-09-17 完成)
- [x] **#36 记录页 V2** — 「这次怎么样？」结果四选(可跳过)+ 白卡原话 + 工具胶囊(加照片 / 记录时间 / 帮我整理 / 关联)+ 生成我的记录;数据契约新增可选 `RecordOutcome`。(2026-09-17 完成)
- [x] **#37 痕迹页 V2** — 统计卡 + 筛选胶囊 + 时间线卡片流;分类为关键词推导。(2026-09-17 完成)
- [x] **#38 分享卡 V2** — 一整屏深色;满幅裁切封面 + 「第 N 次尝试」徽章 + 时长大字 + 正文 + 签名;保留原话开关;分享(Web Share,不支持则回落保存)/ 只保存图片。(2026-09-17 完成)
- [x] **#39 底部导航 + 我的页 V2** — 四槽深色胶囊 + 白胶囊指示;外观页重排为「我的」,草稿模型不变。(2026-09-17 完成)
- [x] **#40 走查取证与文档同步** — typecheck / build 通过;闭环走查 11 张截图;DESIGN.md 换写 V2。(2026-09-17 完成)

**裁决进展(2026-09-17)**:用户已定 ② **卡片不裁切**(维持 #16,分享卡改深色 + 文字在封面下方)、③ **筛选去掉**(分类推导与其实现一并删除)。
用户又定 ④ **底部不放「我的」**——今天页右上头像已是入口,导航落为三槽(今天 / 开始 / 痕迹)。
最后一条 ① **行动页出口** 也定了:**方案 C = 保留三选、不复刻「换一个建议」**。四处冲突已全部裁决,工件已回写(DESIGN §2.1/§3/§4、plan.md、build.md、test.md)。

## 六期(2026-09-19 立项,范围:V2 真机反馈)

> 五期 V2 的 test 只覆盖到桌面 Chromium 模拟视口，「真机走查」一直挂在 maintain 的 Follow-up 里没做；用户真机上手后报出 1 个阻断 + 2 处体验问题 + 1 项待讨论的改版。六期先修阻断，再收表达；卡片改版单独讨论后再动手。多窗口认领规则同一期。

- [ ] **#42 第一步页返回卡死(阻断)** — 从「正在进行」卡进入第一步页(编辑既有 DO)后点左上「返回」，页面被弹回第一步页，观感即「返回键一直卡」。根因：返回按钮把屏幕设回对话页(`setScreen('input')`)，而对话页**挂载即自动重发首页念头**(InputPage 的 seed effect)，AI 落定后 `setScreen('action')` 又推回第一步页——每次返回都夹一次真实 AI 请求，形成「返回 → 弹回」循环。修法：第一步页的返回不再回对话页，改走 `useExpandTransition().leave()` 收回首页(与「放弃这一步」同义)，念头与 activeDOId 按既有 `finalizeLeave` 清理。判据：编辑态点返回必须落到今天页且不再重发请求。
- [ ] **#43 今天 / 痕迹横滑手势失效** — 手指左右滑不动(指示胶囊与程序化滚动都正常)。根因：App 里 `tab → 分页位置` 的 effect 在「越过中点立刻翻」时会 `pager.scrollTo({behavior:'smooth'})`，而这次 tab 变化正是**用户手势**触发的；触摸拖拽期间调用程序化滚动会打断/取消原生触摸滚动，表现为「滑一下就弹回」。四期 `interaction-motion-h5/test.md` 的 Untested 第一条已点名「真触摸串(pointerdown→move→up 的 scroll-snap 拖拽)未覆盖」，当时只用 `scrollLeft=430` 验证了吸附。修法：分清 tab 变化的来源(手势 / 点 Tab)，手势驱动的那一次只让状态跟上、绝不介入滚动位置。
- [ ] **#44 念头卡去掉示例 chip** — 首页「一个模糊的念头」卡里的三个推荐示例(跑步 10 分钟 / 做一顿饭 / 拍一张照片)去掉，只留输入框与「帮我找到第一步」。理由(用户 2026-09-19)：推荐词把「模糊的念头」收窄成三个固定答案，与「写一句就行」相抵。连带删 `EXAMPLE_IDEAS`、`.idea-chips` 样式，DESIGN §3 同步。
- [ ] **#45 痕迹导出卡片改版(待讨论，本轮不动手)** — 用户 2026-09-19：「痕迹导出的卡片可能要重新设计一下样式或者模板」，并明确「最后做，卡片设计到时候再讨论」。**动手前先开一轮讨论并落 `CONTEXT.md` / `DESIGN.md`**；待讨论维度见 change `v2-device-feedback` 的 plan.md「Open decisions」。

## SDLC 流程修复(2026-09-17 立项)

> intent:修审计发现的四个缺口——空证据放行、学习零沉淀、无运行日志、三期零工件;同步验证 SDLC skill 本身(用户正在开发中测试)。建议 #19 → #20 → #21/#22 顺序,#21 与 #22 可并行。

- [ ] **#19 补录二期空 test.md** — record-writing-h5 / record-card-export-h5 / ai-dialogue-planning-h5 三个 test.md 是空模板但已放行;按各自 build.md、git 提交(6ad5ecc / 0ef2d57 / f40158b)、部署 run 34763304466 还原验收标准与结果,顶部标注「as-built 补录,2026-09-14」。
- [ ] **#20 事故根因与防复发计划**(前置 #19)— 每起事故产出「根因结论 + 防复发去向」,无 durable control 不得勾选;按 maintain 路由写入 .sdlc/INDEX.md「Recent Learnings」:
  - **531b570 页面切换卡死**:根因待查清(为何 AnimatePresence 退出依赖会卡死——退出动画与页面卸载的时序?);防复发 = 动效契约写入 DESIGN(页面切换禁用退出动画依赖)+ 切换场景列入真机走查清单
  - **0ef2d57 html-to-image 挂起**:根因未诊断(嵌入式 webview 下为何挂起——跨域图片 promise 不 resolve?),短期防复发 = 契约写死「导出一律 canvas 手绘,禁用 DOM 截图库」,未来重引入截图前必须先诊断
  - **2b6f391 FileList 静默失败**:根因已知(异步处理器读实时引用被释放);防复发 = 契约加「事件对象必须在处理器内同步快照」+ 多图上传列入走查清单
  - **流程事故**:三期 #16-#18 零 SDLC 工件直接执行;根因 = skill 未介入也无兜底;防复发 = #21 日报约定 + #22 非空约束,并在三期任务旁注明豁免/补录方式
- [x] **#21 建立运行日志 DAILY.md**（2026-09-19 完成，change `sdlc-toolbox-alignment`）— 已落 `.sdlc/DAILY.md`（铁律 + 模板 + 当日记录）、`.sdlc/RUNBOOK.md` §8 指向它、`INDEX.md` Entry Points 挂入口、`AGENTS.md` 加一句约定「每轮会话结束前追加一节」。
- [ ] **#41 SDLC 工具链对齐**（2026-09-19 立项，change `sdlc-toolbox-alignment`）— 已完成的部分见该 change 的 build/test；**剩下的都是工具箱行为改动，需单独评估上游语义**：① gate 词表写进 SKILL（现在只活在脚本字典里，实测传错即拒）；② 加「等待人工」状态（词表里没有，只能沿用旧 gate）；③ `advance` 不沿用上一阶段的 gate；④ `refresh-index` 只重写 `<!-- generated -->` 标记内（现在会覆盖 INDEX 手写段）；⑤ DAILY 模板进工具箱 + navigator 约定；⑥ 工件非空校验脚本（对应 #22 的机械校验，目前只有 RUNBOOK §5 的文字规则）；⑥b `add-artifact` 覆盖已存在工件的问题**已在本机修**（加守卫，见 `.sdlc/RUNBOOK.md` §6）——该项已完成；⑦ `.sdlc/learnings/`、`.sdlc/decisions/` 空目录去留；⑧ 模板去掉 `status:`（消除与 lifecycle.yaml 双写漂移，本轮已见实例）；⑨ 同文 hash 校验可做成 pre-commit。
  来源清单与证据：`CLAUDE_SDLC_TODO.md`——**该文件是 SDLC 优化的长期台账**（用户 2026-09-19 指定单独保留，专门用于优化 SDLC），本条目与其同步维护。
- [ ] **#22 gate 证据非空约束** — .sdlc/INDEX.md「Active Constraints」加一条:gate 放行前该阶段工件必须非空(验收表有行、证据链接有目标);AGENTS.md 同步一句;后续可给 inspect_sdlc_state.py 外加非空校验脚本机械化。

## 数据契约(所有窗口共用;谁改谁同步本文件并提交)

```text
DO     { id, thought, action{title, time, stop},
         status: 待定|待记录|已记录, intent: 愿意去做|不想做了|暂不决定|null,
         createdAt, parkedAt }
Record { id, text, refined?, images[], outcome?, linkedDOId?|null, createdAt }
  outcome?(V2 新增,可选): 做完了|做了一部分|中途停下来了|发现不太适合我
Settings { theme, background, onboarded?(V2 新增), ai? }
  (编辑态显式 linkedDOId: null = 移除关联,reducer 删键;#13)

状态流转:创建→待定;开始行动→待记录;保存关联记录→已记录;编辑移除关联→该 DO 回退待记录(#13)。不存在「已完成」。
```
