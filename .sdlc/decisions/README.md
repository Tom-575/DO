# decisions

需要长期留档、但**还没成形为 ADR** 的决策（取舍、口径、边界）。

规则：

- 已成形的决策请写 ADR（`docs/design/` 或项目约定的 ADR 目录），并回到本目录留一个链接；
- 只在某次变更内有意义的决定，写进该 change 的 `plan.md` / `design.md`，不要放这里；
- 目录为空 = 目前没有这一类决策，**不代表这个目录没用**（空目录在 Git 里根本不存在，所以留这页说明）。
