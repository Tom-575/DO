# learnings

跨变更、**可复用**的结论（一条学习在这里出现，意味着它已经过了至少一次真实验证）。

规则：

- 单次变更的学习先写进该 change 的 `maintain.md`，并在 `.sdlc/INDEX.md` 的 Recent Learnings 留一行；
- 确认可复用后，再在这里落成独立文件（一个主题一个文件，文件名用 `kebab-case`）；
- 目录为空 = 目前没有已验证可复用的学习，**不代表这个目录没用**（空目录在 Git 里根本不存在，所以留这页说明）。
